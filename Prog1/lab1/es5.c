#include <stdio.h>
#include <limits.h>

int main(void) {
    printf("Il range di int e' da %d a %d \n", INT_MIN, INT_MAX);
    printf("Il range di char e' da %d a %d", CHAR_MIN, CHAR_MAX);

    return 0;
}
